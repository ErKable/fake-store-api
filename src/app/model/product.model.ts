export interface IRating {
    rate: number,
    count: number
}

export interface IProduct {
    id: number,
    title: string,
    description: string,
    category: string,
    image: string,
    price: number
}